#include "main.h"

/**
 * _abs - Computes the absolute value of an integer
 * @n: The number to convert into absolute
 *
 * Return: The absolute number.
 */
int _abs(int n)
{
if (n < 0)
{
return (n * -1);
}
return (n);
}
