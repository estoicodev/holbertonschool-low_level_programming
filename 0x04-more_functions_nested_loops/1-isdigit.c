#include "main.h"

/**
 * _isdigit -  checks for digit character.
 * Return: On success 1.
 * Otherwise, 0
 */
int _isdigit(int c)
{
int i;
if (c >= 48 && c <= 57)
{
return (1);
}
else
{
return (0);
}
}
